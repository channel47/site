---
/**
 * Homepage - Founder Journey Landing Page (V6)
 *
 * Refined editorial design with authentic Claude Code terminal.
 * Unified typography, wider content, reserved CTAs.
 *
 * Changes from V5:
 * - Wider content area on desktop for breathing room
 * - Authentic Claude Code terminal showing conversational interaction
 * - Simplified, inline email optins
 * - Unified headline typography (fewer font/color variations)
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Prose from '../components/Prose.astro';
import EmailSignup from '../components/EmailSignup.astro';
import LogoAnimated from '../components/LogoAnimated.astro';
import SocialLinks from '../components/SocialLinks.astro';
---

<BaseLayout
  title="Channel47 — Claude Code plugins for media buyers"
  description="10,000 lines of code a day. I don't know how to code. Free Claude Code plugins for Google Ads, from someone who can't code."
>
  <main class="page">
    <!-- Atmospheric orb - subtle warmth behind content -->
    <div class="atmosphere" aria-hidden="true">
      <div class="atmosphere__orb"></div>
    </div>

    <Prose width="wide">
      <!-- Hero: Massive headline -->
      <header class="hero">
        <p class="hero__overline" data-animate="fade-up" data-stagger="1">Claude Code Plugins for Media Buyers</p>
        <h1 class="hero__headline" data-animate="blur-in" data-stagger="2">
          10,000 lines of code a day.
          <span class="hero__headline-break">I don't know how to code.</span>
        </h1>

        <!-- Claude Code terminal - authentic conversational interface -->
        <div class="claude-terminal" data-animate="fade-up" data-stagger="3">
          <div class="claude-terminal__chrome">
            <div class="claude-terminal__dots">
              <span></span><span></span><span></span>
            </div>
            <div class="claude-terminal__title">claude</div>
          </div>
          <div class="claude-terminal__content">
            <!-- User message -->
            <div class="claude-terminal__message claude-terminal__message--user">
              <span class="claude-terminal__prompt">›</span>
              <span class="claude-terminal__text">Audit all my Google Ads accounts and fix any RSA compliance issues</span>
            </div>

            <!-- Claude thinking/working -->
            <div class="claude-terminal__message claude-terminal__message--claude">
              <div class="claude-terminal__claude-indicator">
                <span class="claude-terminal__claude-icon">●</span>
                <span class="claude-terminal__claude-name">Claude</span>
              </div>
              <div class="claude-terminal__response">
                <p class="claude-terminal__response-text">I'll audit your accounts for RSA compliance. Let me connect to the Google Ads API and scan your campaigns.</p>
                <div class="claude-terminal__tool">
                  <span class="claude-terminal__tool-label">Read</span>
                  <span class="claude-terminal__tool-target">8 accounts · 47 campaigns · 156 RSAs</span>
                </div>
                <p class="claude-terminal__response-text">Found 12 ads missing required headlines. I can fix these now—want me to proceed?</p>
              </div>
            </div>

            <!-- User approval -->
            <div class="claude-terminal__message claude-terminal__message--user">
              <span class="claude-terminal__prompt">›</span>
              <span class="claude-terminal__text">yes</span>
            </div>

            <!-- Claude completing -->
            <div class="claude-terminal__message claude-terminal__message--claude">
              <div class="claude-terminal__claude-indicator">
                <span class="claude-terminal__claude-icon">●</span>
                <span class="claude-terminal__claude-name">Claude</span>
              </div>
              <div class="claude-terminal__response">
                <div class="claude-terminal__tool claude-terminal__tool--success">
                  <span class="claude-terminal__tool-label">Edit</span>
                  <span class="claude-terminal__tool-target">12 RSAs updated across 4 accounts</span>
                </div>
                <p class="claude-terminal__response-text">Done. All accounts now compliant.</p>
              </div>
            </div>
          </div>
          <div class="claude-terminal__status">
            <span class="claude-terminal__status-model">claude-sonnet</span>
            <span class="claude-terminal__status-separator">·</span>
            <span class="claude-terminal__status-branch">main</span>
          </div>
        </div>
        <p class="hero__terminal-caption">8 accounts. 47 campaigns. 156 RSAs. 12 fixed. While I drank my coffee.</p>
      </header>

      <!-- CTA: Inline, reserved -->
      <div class="cta cta--inline" data-animate="fade-up" data-stagger="4">
        <p class="cta__text">Get the plugins free.</p>
        <EmailSignup placeholder="your@email.com" tag="hero" />
        <p class="cta__hint">You just need Claude Pro (~$20/mo)</p>
      </div>

      <div class="section-break" data-animate="fade-in" data-stagger="5" aria-hidden="true"></div>

      <!-- Origin: Dense story block -->
      <section class="story story--origin" data-animate="fade-up" data-stagger="5">
        <p class="story__lede">
          A year ago I lived inside the Google Ads UI. Twenty-five accounts. Hundreds of campaigns. The same checks, over and over.
        </p>
        <p>
          Then I started experimenting with Claude. Not for ads at first. I was curious. I asked it to pull some data. It wrote a script. I asked for another. That worked too.
        </p>
        <p class="story__pull">
          I kept asking. It kept building.
        </p>
        <p>
          Somewhere along the way I stopped thinking of Claude as a tool and started thinking of it as the developer I could never afford. One who writes thousands of lines while I describe what I need in plain English.
        </p>
        <p>
          Now my workflow lives in the terminal. That probably sounds intimidating. It's not. It's a text window. You type what you want. Claude does the rest.
        </p>
      </section>

      <div class="section-break" aria-hidden="true"></div>

      <!-- The Build: Sparse, breathing section with Q&A -->
      <section class="story story--build" data-animate="fade-up" data-stagger="6">
        <h2 class="story__heading">What I'm building</h2>
        <p>
          An open source marketplace for Claude Code plugins. Tools that let Claude work directly with your ad accounts, your data, your workflows.
        </p>
        <p>
          Built for media buyers. Everything free. You just need Claude Pro (~$20/mo).
        </p>
      </section>

      <section class="faq" data-animate="fade-up" data-stagger="7">
        <div class="faq__item">
          <h3 class="faq__question">Why free?</h3>
          <p class="faq__answer">
            I don't need your money. I need your feedback. Use these tools. Break them. Tell me what's broken. That's the deal.
          </p>
        </div>
        <div class="faq__item">
          <h3 class="faq__question">What about my accounts?</h3>
          <p class="faq__answer">
            These plugins can read and modify your data, but only when you ask. Claude shows you what it's about to do and waits for approval. You stay in control.
          </p>
        </div>
      </section>

      <div class="section-break" aria-hidden="true"></div>

      <!-- The Belief: Sparse, worldview statement -->
      <section class="story story--belief" data-animate="fade-up" data-stagger="8">
        <p class="story__manifesto">
          Intelligence is becoming a commodity. Anyone can access Claude for $20 a month. What matters now is the workflows. The prompts. The patterns that produce results.
        </p>
        <p class="story__aside">
          I'm not an expert on these tools. I'm a daily practitioner. Someone who optimizes systems because I can't help myself. Someone who'd rather spend an hour automating a task than do it twice.
        </p>
        <p>
          If you can describe what you want in a sentence, you can use these tools.
        </p>
      </section>

      <!-- Final CTA: Simple, clear -->
      <section class="invitation" data-animate="fade-up" data-stagger="9">
        <div class="invitation__content">
          <h2 class="invitation__headline">Join the list</h2>
          <ul class="invitation__benefits">
            <li>First access to every plugin I ship</li>
            <li>Notes on what's working and what isn't</li>
            <li>No pitch decks. No courses.</li>
          </ul>
          <div class="invitation__form">
            <EmailSignup placeholder="your@email.com" tag="manifesto" />
          </div>
          <p class="invitation__cadence">
            I email when I have something worth saying. Once or twice a month.
          </p>
        </div>
      </section>

      <!-- Footer: Minimal, breathing -->
      <footer class="page-footer" data-animate="fade-up" data-stagger="10">
        <div class="page-footer__logo">
          <LogoAnimated delay={200} />
        </div>
        <div class="page-footer__social">
          <SocialLinks />
        </div>
      </footer>
    </Prose>
  </main>
</BaseLayout>

<style>
  /* =========================================
   * PAGE LAYOUT
   * Full viewport, generous whitespace
   * ========================================= */
  .page {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    min-height: 100vh;
    min-height: 100dvh;
    padding-block-start: max(var(--space-12), env(safe-area-inset-top, 0px) + var(--space-6));
    padding-block-end: max(var(--space-16), env(safe-area-inset-bottom, 0px) + var(--space-10));
    position: relative;
    isolation: isolate;
  }

  @media (min-width: 480px) {
    .page {
      padding-block-start: var(--space-16);
      padding-block-end: var(--space-20);
    }
  }

  @media (min-width: 768px) {
    .page {
      padding-block-start: var(--space-20);
      padding-block-end: var(--space-24);
    }
  }

  @media (min-width: 1024px) {
    .page {
      padding-block-start: var(--space-24);
    }
  }

  @media (min-height: 800px) {
    .page {
      padding-block-start: 8vh;
    }
  }

  /* =========================================
   * ATMOSPHERE
   * Subtle orb effect - positioned behind terminal
   * ========================================= */
  .atmosphere {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: -1;
    overflow: hidden;
  }

  .atmosphere__orb {
    position: absolute;
    top: 10%;
    left: 50%;
    transform: translateX(-50%);
    width: min(900px, 130vw);
    height: min(700px, 90vh);
    background: radial-gradient(
      ellipse 100% 100% at 50% 30%,
      var(--color-accent-glow) 0%,
      oklch(18% 0.02 35 / 0.1) 40%,
      transparent 70%
    );
    filter: blur(120px);
    animation: orb-drift 25s var(--ease-in-out) infinite;
  }

  @keyframes orb-drift {
    0%, 100% {
      opacity: 0.4;
      transform: translateX(-50%) translateY(0);
    }
    50% {
      opacity: 0.6;
      transform: translateX(-50%) translateY(-20px);
    }
  }

  /* =========================================
   * HERO
   * Unified typography: serif headlines, muted second line
   * ========================================= */
  .hero {
    text-align: left;
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .hero {
      margin-bottom: var(--space-14);
    }
  }

  .hero__overline {
    font-family: var(--font-sans);
    font-size: var(--text-2xs);
    font-weight: var(--font-medium);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    color: var(--color-fg-muted);
    margin: 0 0 var(--space-4) 0;
  }

  @media (min-width: 768px) {
    .hero__overline {
      font-size: var(--text-xs);
      margin-bottom: var(--space-5);
    }
  }

  /* Headline - clean serif, reduced color variation */
  .hero__headline {
    font-family: var(--font-serif);
    font-size: clamp(2.25rem, 7vw, 4.5rem);
    font-weight: var(--font-bold);
    letter-spacing: -0.03em;
    line-height: 1.05;
    color: var(--color-fg);
    margin: 0 0 var(--space-10) 0;
    max-width: 18ch;
  }

  .hero__headline-break {
    display: block;
    color: var(--color-fg-secondary);
    margin-top: 0.05em;
  }

  @media (min-width: 768px) {
    .hero__headline {
      font-size: clamp(3rem, 5.5vw, 5rem);
      margin-bottom: var(--space-12);
    }
  }

  .hero__terminal-caption {
    font-family: var(--font-serif);
    font-size: var(--text-sm);
    font-style: italic;
    color: var(--color-fg-muted);
    margin: var(--space-4) 0 0 0;
  }

  @media (min-width: 768px) {
    .hero__terminal-caption {
      font-size: var(--text-base);
      margin-top: var(--space-5);
    }
  }

  /* =========================================
   * CLAUDE CODE TERMINAL
   * Authentic conversational interface
   * ========================================= */
  .claude-terminal {
    max-width: 560px;
    background: var(--color-bg-deep);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
  }

  @media (min-width: 768px) {
    .claude-terminal {
      font-size: var(--text-sm);
    }
  }

  /* Chrome bar */
  .claude-terminal__chrome {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-2) var(--space-3);
    background: var(--color-bg-elevated);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .claude-terminal__dots {
    display: flex;
    gap: 5px;
  }

  .claude-terminal__dots span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .claude-terminal__dots span:nth-child(1) {
    background: #ff5f57;
  }

  .claude-terminal__dots span:nth-child(2) {
    background: #ffbd2e;
  }

  .claude-terminal__dots span:nth-child(3) {
    background: #28c840;
  }

  .claude-terminal__title {
    font-size: var(--text-xs);
    color: var(--color-fg-subtle);
    letter-spacing: 0.02em;
  }

  /* Content area */
  .claude-terminal__content {
    padding: var(--space-4);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  @media (min-width: 768px) {
    .claude-terminal__content {
      padding: var(--space-5);
      gap: var(--space-5);
    }
  }

  /* Messages */
  .claude-terminal__message {
    display: flex;
    gap: var(--space-2);
  }

  .claude-terminal__message--user {
    align-items: flex-start;
  }

  .claude-terminal__message--claude {
    flex-direction: column;
    gap: var(--space-2);
  }

  /* User prompt */
  .claude-terminal__prompt {
    color: var(--color-accent);
    font-weight: var(--font-medium);
    flex-shrink: 0;
    user-select: none;
  }

  .claude-terminal__text {
    color: var(--color-fg);
    line-height: var(--leading-relaxed);
  }

  /* Claude indicator */
  .claude-terminal__claude-indicator {
    display: flex;
    align-items: center;
    gap: var(--space-2);
  }

  .claude-terminal__claude-icon {
    color: var(--color-accent);
    font-size: 0.6em;
  }

  .claude-terminal__claude-name {
    font-size: var(--text-xs);
    font-weight: var(--font-medium);
    color: var(--color-fg-muted);
    letter-spacing: 0.01em;
  }

  /* Claude response */
  .claude-terminal__response {
    padding-left: var(--space-4);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .claude-terminal__response-text {
    color: var(--color-fg-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
    font-size: 0.95em;
  }

  /* Tool indicators */
  .claude-terminal__tool {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-1) var(--space-2);
    background: var(--color-bg-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    width: fit-content;
  }

  .claude-terminal__tool-label {
    font-size: 0.85em;
    font-weight: var(--font-semibold);
    color: var(--color-fg-muted);
  }

  .claude-terminal__tool-target {
    font-size: 0.85em;
    color: var(--color-fg-subtle);
  }

  .claude-terminal__tool--success .claude-terminal__tool-label {
    color: var(--color-accent);
  }

  /* Status bar */
  .claude-terminal__status {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--color-bg-elevated);
    border-top: 1px solid var(--color-border-subtle);
    font-size: var(--text-2xs);
    color: var(--color-fg-subtle);
  }

  .claude-terminal__status-model {
    color: var(--color-accent);
  }

  .claude-terminal__status-separator {
    opacity: 0.5;
  }

  /* =========================================
   * INLINE CTA
   * Reserved, inline with content
   * ========================================= */
  .cta--inline {
    margin-bottom: var(--space-14);
    max-width: 340px;
  }

  @media (min-width: 768px) {
    .cta--inline {
      margin-bottom: var(--space-16);
    }
  }

  .cta__text {
    font-family: var(--font-serif);
    font-size: var(--text-base);
    color: var(--color-fg);
    margin: 0 0 var(--space-4) 0;
  }

  @media (min-width: 768px) {
    .cta__text {
      font-size: var(--fluid-lg);
    }
  }

  .cta__hint {
    font-family: var(--font-sans);
    font-size: var(--text-xs);
    color: var(--color-fg-subtle);
    margin: var(--space-3) 0 0 0;
  }

  /* =========================================
   * SECTION BREAK
   * Minimal, breathing space
   * ========================================= */
  .section-break {
    height: 1px;
    background: linear-gradient(
      90deg,
      var(--color-border) 0%,
      var(--color-border-subtle) 100%
    );
    margin-block: var(--space-12);
    max-width: 120px;
  }

  @media (min-width: 768px) {
    .section-break {
      margin-block: var(--space-16);
    }
  }

  /* =========================================
   * STORY SECTIONS
   * Unified serif typography
   * ========================================= */
  .story {
    max-width: var(--measure-prose);
  }

  .story p {
    font-family: var(--font-serif);
    font-size: var(--type-prose-size);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-secondary);
    margin: 0 0 var(--space-5) 0;
  }

  .story p:last-child {
    margin-bottom: 0;
  }

  @media (min-width: 768px) {
    .story p {
      font-size: var(--fluid-lg);
      line-height: var(--leading-prose);
    }
  }

  /* Origin: Dense, narrative block */
  .story--origin {
    margin-bottom: var(--space-4);
  }

  .story__lede {
    font-size: var(--fluid-lg) !important;
    color: var(--color-fg) !important;
    font-weight: var(--font-medium);
  }

  @media (min-width: 768px) {
    .story__lede {
      font-size: var(--fluid-xl) !important;
    }
  }

  .story__pull {
    font-size: var(--fluid-xl) !important;
    font-weight: var(--font-medium);
    color: var(--color-fg) !important;
    padding: var(--space-4) 0;
    margin: var(--space-4) 0 !important;
    border-top: 1px solid var(--color-border-subtle);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  @media (min-width: 768px) {
    .story__pull {
      font-size: var(--fluid-2xl) !important;
      padding: var(--space-6) 0;
      margin: var(--space-6) 0 !important;
    }
  }

  /* Build section: Sparse, heading-led */
  .story--build {
    margin-bottom: var(--space-8);
  }

  .story__heading {
    font-family: var(--font-serif);
    font-size: var(--fluid-lg);
    font-weight: var(--font-semibold);
    color: var(--color-fg);
    margin: 0 0 var(--space-4) 0;
  }

  @media (min-width: 768px) {
    .story__heading {
      font-size: var(--fluid-xl);
      margin-bottom: var(--space-5);
    }
  }

  /* Belief section: Manifesto tone */
  .story--belief {
    padding: var(--space-8) 0;
  }

  @media (min-width: 768px) {
    .story--belief {
      padding: var(--space-12) 0;
    }
  }

  .story__manifesto {
    font-size: var(--fluid-lg) !important;
    font-weight: var(--font-medium);
    color: var(--color-fg) !important;
    line-height: var(--leading-snug) !important;
  }

  @media (min-width: 768px) {
    .story__manifesto {
      font-size: var(--fluid-xl) !important;
    }
  }

  .story__aside {
    font-style: italic;
    color: var(--color-fg-muted) !important;
  }

  /* =========================================
   * FAQ SECTION
   * Compact Q&A pairs
   * ========================================= */
  .faq {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    max-width: var(--measure-prose);
  }

  @media (min-width: 768px) {
    .faq {
      gap: var(--space-8);
    }
  }

  .faq__item {
    padding-left: var(--space-4);
    border-left: 2px solid var(--color-border-subtle);
  }

  .faq__question {
    font-family: var(--font-serif);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    color: var(--color-fg);
    margin: 0 0 var(--space-2) 0;
  }

  @media (min-width: 768px) {
    .faq__question {
      font-size: var(--fluid-lg);
    }
  }

  .faq__answer {
    font-family: var(--font-serif);
    font-size: var(--type-prose-size);
    line-height: var(--leading-relaxed);
    color: var(--color-fg-secondary);
    margin: 0;
  }

  @media (min-width: 768px) {
    .faq__answer {
      font-size: var(--fluid-md);
    }
  }

  /* =========================================
   * INVITATION
   * Simple, clean - no card treatment
   * ========================================= */
  .invitation {
    margin-top: var(--space-16);
  }

  @media (min-width: 768px) {
    .invitation {
      margin-top: var(--space-24);
    }
  }

  .invitation__content {
    max-width: var(--measure-prose);
  }

  .invitation__headline {
    font-family: var(--font-serif);
    font-size: var(--fluid-2xl);
    font-weight: var(--font-semibold);
    color: var(--color-fg);
    margin: 0 0 var(--space-5) 0;
  }

  @media (min-width: 768px) {
    .invitation__headline {
      font-size: var(--fluid-3xl);
    }
  }

  .invitation__benefits {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6) 0;
  }

  .invitation__benefits li {
    font-family: var(--font-serif);
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    padding: var(--space-1-5) 0;
  }

  @media (min-width: 768px) {
    .invitation__benefits li {
      font-size: var(--text-base);
    }
  }

  .invitation__form {
    margin-bottom: var(--space-4);
    max-width: 320px;
  }

  .invitation__cadence {
    font-family: var(--font-sans);
    font-size: var(--text-xs);
    color: var(--color-fg-subtle);
    margin: 0;
  }

  /* =========================================
   * FOOTER
   * Minimal, lots of breathing room
   * ========================================= */
  .page-footer {
    margin-top: var(--space-24);
    padding-top: var(--space-10);
    border-top: 1px solid var(--color-border-subtle);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-6);
  }

  @media (min-width: 768px) {
    .page-footer {
      margin-top: var(--space-32);
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      gap: var(--space-8);
    }
  }

  @media (min-width: 1024px) {
    .page-footer {
      margin-top: 10rem;
      padding-top: var(--space-12);
    }
  }

  .page-footer__logo {
    opacity: 0.6;
    transition: opacity var(--duration-normal) var(--ease-out);
  }

  .page-footer__logo:hover {
    opacity: 1;
  }

  .page-footer__social {
    opacity: 0.35;
    transition: opacity var(--duration-normal) var(--ease-out);
  }

  .page-footer__social:hover {
    opacity: 0.6;
  }

  /* =========================================
   * REDUCED MOTION
   * ========================================= */
  @media (prefers-reduced-motion: reduce) {
    .atmosphere__orb {
      animation: none;
      opacity: 0.4;
    }
  }
</style>
