---
/**
 * Homepage — AI plugins for media buyers.
 *
 * Density: LOW (hero) → HIGH (proof bar) → HIGH (plugin directory) →
 *          MEDIUM (credibility) → RUPTURE → MEDIUM (product) → LOW (CTA void)
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import EmailSignup from '../components/EmailSignup.astro';
import ProductCallout from '../components/ProductCallout.astro';

const schema = [
  {
    '@type': 'Organization',
    'name': 'Channel 47',
    'url': 'https://channel47.dev',
    'logo': 'https://channel47.dev/og-image.png',
    'description': 'AI plugins for media buyers. Platform-specific plugins for Paid Search, Meta Ads, and TikTok.',
    'founder': {
      '@type': 'Person',
      'name': 'Jackson Dean',
      'url': 'https://x.com/ctrlswing',
    },
    'sameAs': [
      'https://github.com/channel47',
      'https://x.com/ctrlswing',
    ],
  },
  {
    '@type': 'WebSite',
    'name': 'Channel 47',
    'url': 'https://channel47.dev',
  },
];
---

<BaseLayout
  title="Channel 47 — AI Plugins for Media Buyers"
  description="Platform-specific AI plugins for Paid Search, Meta Ads, and TikTok. Install and run. Build your own."
  schema={schema}
>
  <Nav />

  <main id="main-content">

    <!-- ═══════ HERO — LOW density ═══════ -->
    <section class="hero is-visible">
      <div class="hero__inner">
        <div class="hero__bar" aria-hidden="true"></div>
        <p class="label hero__label">Transmitting</p>
        <h1 class="hero__headline">AI plugins for<br />media buyers.</h1>
        <p class="hero__sub">
          Platform-specific plugins for Paid Search, Meta Ads, and TikTok.<br />
          Install and run. Or learn to build your own.
        </p>
        <div class="hero__cta">
          <EmailSignup variant="inline" placeholder="you@domain.com" tag="home" submitText="Get Build Notes" />
          <p class="hero__hint">One email a week. Unsubscribe anytime.</p>
        </div>
      </div>
    </section>

    <!-- ═══════ PROOF BAR — HIGH density ═══════ -->
    <section class="proof-bar" data-reveal-stagger>
      <div class="proof-bar__item" data-reveal-child data-stagger="1">
        <span class="proof-bar__value" data-counter="25">25<span class="text-signal">+</span></span>
        <span class="proof-bar__label">Accounts Managed</span>
      </div>
      <div class="proof-bar__item" data-reveal-child data-stagger="2">
        <span class="proof-bar__value proof-bar__value--signal" data-counter="3">3</span>
        <span class="proof-bar__label">Platforms</span>
      </div>
      <div class="proof-bar__item" data-reveal-child data-stagger="3">
        <span class="proof-bar__value">Weekly</span>
        <span class="proof-bar__label">Updates</span>
      </div>
      <div class="proof-bar__item" data-reveal-child data-stagger="4">
        <span class="proof-bar__value">500+</span>
        <span class="proof-bar__label">Builders</span>
      </div>
    </section>

    <!-- ═══════ PLUGIN DIRECTORY — HIGH density ═══════ -->
    <section class="plugin-dir" data-reveal-stagger>
      <div class="wrap">
        <div class="plugin-dir__header" data-reveal-child data-stagger="1">
          <h2 class="plugin-dir__title">Browse the plugins</h2>
          <a href="/plugins" class="plugin-dir__viewall">View all →</a>
        </div>

        <ul class="plugin-dir__list" data-reveal-child data-stagger="2">
          <li class="plugin-dir__row">
            <a href="/plugins/paid-search" class="plugin-dir__link">
              <span class="plugin-dir__idx">001</span>
              <span class="plugin-dir__name">Paid Search</span>
              <span class="plugin-dir__badge plugin-dir__badge--live">Live</span>
              <span class="plugin-dir__arrow" aria-hidden="true">→</span>
            </a>
          </li>
          <li class="plugin-dir__row">
            <a href="/coming-soon" class="plugin-dir__link">
              <span class="plugin-dir__idx">002</span>
              <span class="plugin-dir__name">Meta Ads</span>
              <span class="plugin-dir__badge plugin-dir__badge--dev">In Dev</span>
              <span class="plugin-dir__arrow" aria-hidden="true">→</span>
            </a>
          </li>
          <li class="plugin-dir__row">
            <a href="/coming-soon" class="plugin-dir__link">
              <span class="plugin-dir__idx">003</span>
              <span class="plugin-dir__name">TikTok Ads</span>
              <span class="plugin-dir__badge plugin-dir__badge--soon">Soon</span>
              <span class="plugin-dir__arrow" aria-hidden="true">→</span>
            </a>
          </li>
        </ul>
      </div>
    </section>

    <!-- ═══════ CREDIBILITY — MEDIUM density ═══════ -->
    <section class="credibility" data-reveal>
      <div class="wrap">
        <p class="label">Built from real accounts</p>
        <p class="credibility__body">
          The paid-search plugin caught ~$3K/month in wasted spend across two accounts. A pacing alert flagged a budget overrun four days before the client noticed. Every tool here comes from managing 25+ accounts daily — not a lab.
        </p>
        <a href="/notes" class="credibility__link">Follow the build notes →</a>
      </div>
    </section>

    <!-- ═══════ RUPTURE — surface flip ═══════ -->
    <section class="rupture" data-reveal>
      <div class="rupture__inner">
        <div class="rupture__bar"></div>
        <p class="label label--muted" style="margin-bottom: 20px;">Signal</p>
        <h2 class="rupture__headline">The people who build skills become the most valuable people in their field.</h2>
        <p class="rupture__body">
          Every tool here was built from a real workflow. Install and run, or learn how it's made and build your own.
        </p>
        <a href="/plugins" class="rupture__link">Browse all tools</a>
      </div>
    </section>

    <!-- ═══════ PRODUCT CALLOUT — MEDIUM density ═══════ -->
    <ProductCallout />

    <!-- ═══════ CTA VOID — LOW density ═══════ -->
    <section class="cta" data-reveal>
      <a href="/plugins" class="cta__headline">Browse the tools.</a>
      <a href="/subscribe" class="btn-signal">Get Build Notes</a>
    </section>

    <Footer />
  </main>
</BaseLayout>

<style>
  /* ═══════ PLUGIN DIRECTORY ═══════ */
  .plugin-dir {
    padding: clamp(72px, 14vh, 140px) 0;
    border-top: 1px solid var(--color-smoke);
  }

  /* Header — title left, view-all right */
  .plugin-dir__header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 32px;
  }
  .plugin-dir__title {
    font-family: var(--font-family-display);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--color-chalk);
  }
  .plugin-dir__viewall {
    font-family: var(--font-family-mono);
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-signal);
    transition: opacity var(--dur-instant) var(--ease-sharp);
  }
  .plugin-dir__viewall:hover { opacity: 0.8; }
  .plugin-dir__viewall:focus-visible {
    outline: 2px solid var(--color-signal);
    outline-offset: 2px;
    border-radius: var(--radius-tight);
  }

  /* List */
  .plugin-dir__list { list-style: none; }

  /* Row */
  .plugin-dir__row {
    border-bottom: 1px solid var(--color-smoke);
  }
  .plugin-dir__row:first-child {
    border-top: 1px solid var(--color-smoke);
  }

  /* Link grid */
  .plugin-dir__link {
    position: relative;
    display: grid;
    grid-template-columns: 3rem 1fr auto auto;
    align-items: center;
    gap: 16px;
    padding: 20px 16px;
    margin: 0 -16px;
    transition: background-color var(--dur-quick) var(--ease-out);
  }
  .plugin-dir__link::before {
    content: '';
    position: absolute;
    left: 0;
    top: 12px;
    bottom: 12px;
    width: 2px;
    background: var(--color-signal);
    transform: scaleY(0);
    transition: transform 100ms var(--ease-sharp);
  }
  .plugin-dir__link:hover {
    background-color: rgba(245, 158, 11, 0.03);
  }
  .plugin-dir__link:hover::before {
    transform: scaleY(1);
  }
  .plugin-dir__link:focus-visible {
    outline: 2px solid var(--color-signal);
    outline-offset: -2px;
    border-radius: var(--radius-tight);
  }

  /* Cells */
  .plugin-dir__idx {
    font-family: var(--font-family-mono);
    font-size: 11px;
    letter-spacing: 0.08em;
    color: var(--color-ash);
  }
  .plugin-dir__name {
    font-family: var(--font-family-mono);
    font-size: 14px;
    color: var(--color-bone);
  }
  .plugin-dir__arrow {
    font-family: var(--font-family-mono);
    font-size: 14px;
    color: var(--color-ash);
    transition: color var(--dur-instant) var(--ease-sharp);
  }
  .plugin-dir__link:hover .plugin-dir__arrow {
    color: var(--color-signal);
  }

  /* Status badges */
  .plugin-dir__badge {
    padding: 3px 8px;
    border-radius: var(--radius-tight);
    font-family: var(--font-family-mono);
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .plugin-dir__badge--live {
    background: var(--color-signal);
    color: var(--color-void);
  }
  .plugin-dir__badge--dev {
    border: 1px solid var(--color-ash);
    color: var(--color-dust);
  }
  .plugin-dir__badge--soon {
    border: 1px solid var(--color-smoke);
    color: var(--color-stone);
  }

  @media (max-width: 480px) {
    .plugin-dir__link {
      grid-template-columns: 1fr auto auto;
    }
    .plugin-dir__idx { display: none; }
  }
</style>

