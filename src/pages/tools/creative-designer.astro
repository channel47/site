---
/**
 * Creative Designer Tool Page
 *
 * Setup guide for the open-source Creative Designer plugin.
 * MIT licensed.
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import EmailSignup from '../../components/EmailSignup.astro';
import InstallTabs from '../../components/InstallTabs.astro';
import ToolSchema from '../../components/SEO/ToolSchema.astro';
import Badge from '../../components/Badge.astro';
---

<BaseLayout
  title="Creative Designer — AI Image Generation | Channel47"
  description="Generate ad creatives with platform specs baked in. Powered by Google Gemini. Open-source and MIT licensed."
>
  <ToolSchema
    name="Creative Designer"
    description="AI image generation with platform specs baked in. Powered by Gemini."
    url="https://channel47.dev/tools/creative-designer"
    version="2.0.0"
  />
  <Header />

  <main class="tool-page">
    <!-- Breadcrumb -->
    <nav class="breadcrumb">
      <a href="/" class="breadcrumb__link">&larr; Back to Tools</a>
    </nav>

    <article class="tool-content">
      <!-- Hero -->
      <header class="tool-header">
        <Badge variant="success" class="tool-header__badge">Free</Badge>
        <h1>Creative Designer</h1>
        <p class="tool-header__intro">
          AI image generation with platform specs baked in. Powered by Google Gemini.
        </p>
      </header>

      <!-- What You'll Need -->
      <section class="tool-section">
        <h2>What you'll need</h2>
        <ul class="requirements-list">
          <li>
            <strong>Claude Code or Claude Desktop</strong>
          </li>
          <li>
            <strong>A Google Gemini API key</strong> (free tier available)
          </li>
          <li>
            <strong>Five minutes</strong>
          </li>
        </ul>
      </section>

      <!-- Installation -->
      <section class="tool-section">
        <h2>Installation</h2>
        <p>
          Choose your setup method below. Creative Designer works as both an MCP server and a Claude Code plugin.
        </p>

        <InstallTabs plugin="creative-designer" />
      </section>

      <!-- Getting Your API Key -->
      <section class="tool-section">
        <h2>Getting your Gemini API key</h2>
        <ol class="numbered-list">
          <li>Go to <a href="https://aistudio.google.com/apikey" target="_blank" rel="noopener">Google AI Studio</a></li>
          <li>Click "Create API key"</li>
          <li>Copy the key and add it to your configuration</li>
        </ol>
        <p>
          The free tier includes generous usage limits for image generation.
        </p>
      </section>

      <!-- Test It -->
      <section class="tool-section">
        <h2>Test it</h2>
        <p>
          Start a new conversation and ask something like:
        </p>
        <div class="test-prompt">
          <p>"Generate a Facebook ad creative for a coffee brand, 1200x628"</p>
        </div>
        <p>
          The tool will generate an image with the correct dimensions and platform specifications.
        </p>
      </section>

      <!-- What's Included -->
      <section class="tool-section section--features">
        <h2>What's included</h2>
        <ul class="feature-list">
          <li>Dual model tiers: Flash (fast, 2-3s, up to 1024px) and Pro (4K quality)</li>
          <li>Smart model selection based on prompt analysis</li>
          <li>7 aspect ratios: 1:1, 16:9, 9:16, 21:9, 4:3, 3:4, 2:1</li>
          <li>Google Search grounding for factually accurate images (Pro only)</li>
          <li>Reference image support via file upload</li>
          <li>Seed-based reproducible generation</li>
          <li>Configurable safety levels</li>
        </ul>
        <p class="feature-note">
          All features are open-source and MIT licensed. <a href="https://github.com/channel47/channel47/tree/main/plugins/creative-designer">View on GitHub</a>
        </p>
      </section>

      <!-- Email Signup -->
      <section class="tool-section section--email">
        <h3 class="email-title">Get updates on this tool.</h3>
        <p class="email-description">New features, fixes, and tips. No spam.</p>
        <EmailSignup buttonText="Subscribe" tag="creative-designer-tool-user" />
      </section>
    </article>
  </main>

  <Footer />
</BaseLayout>

<style>
  .tool-page {
    padding: var(--space-6) var(--padding-mobile);
    padding-top: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-page {
      padding: var(--space-10) var(--padding-desktop);
      padding-top: var(--space-6);
    }
  }

  /* Breadcrumb */
  .breadcrumb {
    max-width: var(--content-width);
    margin: 0 auto var(--space-6);
  }

  .breadcrumb__link {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .breadcrumb__link:hover {
    color: var(--color-fg);
  }

  .tool-content {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  /* Tool Header */
  .tool-header {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-header {
      margin-bottom: var(--space-12);
    }
  }

  .tool-header__badge {
    margin-bottom: var(--space-4);
  }

  .tool-header h1 {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-3);
  }

  @media (min-width: 768px) {
    .tool-header h1 {
      font-size: var(--text-4xl);
      margin-bottom: var(--space-4);
    }
  }

  .tool-header__intro {
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    margin: 0;
  }

  @media (min-width: 768px) {
    .tool-header__intro {
      font-size: var(--text-xl);
    }
  }

  /* Sections */
  .tool-section {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-section {
      margin-bottom: var(--space-12);
    }
  }

  .tool-section h2 {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-section h2 {
      font-size: var(--text-2xl);
    }
  }

  .tool-section p {
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  /* Requirements List */
  .requirements-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .requirements-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  .requirements-list li::before {
    content: "\2192";
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  /* Numbered List */
  .numbered-list {
    padding-left: var(--space-6);
    margin: 0 0 var(--space-4) 0;
  }

  .numbered-list li {
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
  }

  .numbered-list a {
    color: var(--color-accent);
  }

  /* Test Prompt */
  .test-prompt {
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    margin: var(--space-4) 0;
  }

  .test-prompt p {
    font-style: italic;
    color: var(--color-fg);
    margin: 0;
  }

  /* Features Section */
  .section--features {
    margin-top: var(--space-10);
  }

  @media (min-width: 768px) {
    .section--features {
      margin-top: var(--space-12);
    }
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6) 0;
  }

  .feature-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
  }

  .feature-list li::before {
    content: "\2713";
    position: absolute;
    left: 0;
    color: var(--color-success);
    font-weight: var(--font-bold);
  }

  .feature-note {
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-md);
    margin-bottom: 0;
  }

  .feature-note a {
    color: var(--color-accent);
  }

  /* Email Section */
  .section--email {
    padding-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .section--email {
      padding-bottom: var(--space-12);
    }
  }

  .email-title {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--color-fg);
    margin-bottom: var(--space-2);
  }

  .email-description {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    margin-bottom: var(--space-4);
  }
</style>
