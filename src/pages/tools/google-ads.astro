---
/**
 * Google Ads MCP Tool Page
 *
 * Setup guide for the open-source Google Ads MCP server.
 * MIT licensed.
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import EmailSignup from '../../components/EmailSignup.astro';
import InstallTabs from '../../components/InstallTabs.astro';
import ToolSchema from '../../components/SEO/ToolSchema.astro';
---

<BaseLayout
  title="Google Ads MCP — Free Setup Guide | Channel47"
  description="Connect the free Google Ads MCP server to Claude or Cursor in five minutes. Query any account. No install required."
>
  <ToolSchema
    name="Google Ads MCP Server"
    description="Query Google Ads data using GAQL with OAuth authentication"
    url="https://channel47.dev/tools/google-ads"
    version="4.0.0"
  />
  <Header />

  <main class="tool-page">
    <!-- Breadcrumb -->
    <nav class="breadcrumb">
      <a href="/tools" class="breadcrumb__link">← Back to Tools</a>
    </nav>

    <article class="tool-content">
      <!-- Hero -->
      <header class="tool-header">
        <div class="tool-header__badge">Free</div>
        <h1>Google Ads MCP</h1>
        <p class="tool-header__intro">
          Connect in five minutes. Query any account. No install required.
        </p>
      </header>

      <!-- What You'll Need -->
      <section class="tool-section">
        <h2>What you'll need</h2>
        <ul class="requirements-list">
          <li>
            <strong>Claude Desktop, Cursor, or another MCP-compatible tool</strong>
          </li>
          <li>
            <strong>Access to your Google Ads account</strong> (you'll authenticate through OAuth)
          </li>
          <li>
            <strong>Five minutes</strong>
          </li>
        </ul>
      </section>

      <!-- Add the MCP -->
      <section class="tool-section">
        <h2>Add the MCP</h2>
        <p>
          Choose your setup method below. The MCP server connects Claude to your Google Ads account.
        </p>

        <InstallTabs plugin="google-ads" />

        <div class="setup-step setup-step--other">
          <h3>For Cursor or other MCP tools</h3>
          <p>
            Add this to your MCP server configuration:
          </p>
          <div class="code-block-wrapper">
            <pre class="code-block"><code>{`{
  "google-ads": {
    "command": "npx",
    "args": ["-y", "@channel47/google-ads-mcp@latest"],
    "env": {
      "GOOGLE_ADS_DEVELOPER_TOKEN": "your-token",
      "GOOGLE_ADS_CLIENT_ID": "your-client-id",
      "GOOGLE_ADS_CLIENT_SECRET": "your-client-secret",
      "GOOGLE_ADS_REFRESH_TOKEN": "your-refresh-token"
    }
  }
}`}</code></pre>
          </div>
        </div>
      </section>

      <!-- Authenticate -->
      <section class="tool-section">
        <h2>Authenticate</h2>
        <p>
          On first use, Channel47 will prompt you to connect your Google Ads account via OAuth.
          Standard Google authentication—we never see your password.
        </p>
        <p>
          You'll grant read access to your account data. That's it.
        </p>
      </section>

      <!-- Test It -->
      <section class="tool-section">
        <h2>Test it</h2>
        <p>
          Start a new conversation and ask something like:
        </p>
        <div class="test-prompt">
          <p>"What's my top spending campaign this month?"</p>
        </div>
        <p>
          If you see data, you're connected. If you see an error, double-check the URL and restart your tool.
        </p>
      </section>

      <!-- What's Included -->
      <section class="tool-section section--features">
        <h2>What's included</h2>
        <ul class="feature-list">
          <li>Analyst subagent that writes GAQL queries for you</li>
          <li>Query isolation — large result sets stay out of main context</li>
          <li>Mutation validation hook — prevents accidental live changes</li>
          <li>Interactive setup wizard for OAuth credential configuration</li>
          <li>Account listing and management</li>
          <li>Troubleshooting skill for common auth and format issues</li>
        </ul>
        <p class="feature-note">
          All features are open-source and MIT licensed. <a href="https://github.com/channel47/channel47/tree/main/plugins/google-ads-specialist">View on GitHub</a>
        </p>
      </section>

      <!-- Email Signup -->
      <section class="tool-section section--email">
        <EmailSignup
          title="Get updates on this tool."
          description="New features, fixes, and tips. No spam."
          buttonText="Subscribe"
          variant="card"
          tag="google-ads-tool-user"
        />
      </section>
    </article>
  </main>

  <Footer />
</BaseLayout>


<style>
  .tool-page {
    padding: var(--space-6) var(--padding-mobile);
    padding-top: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-page {
      padding: var(--space-10) var(--padding-desktop);
      padding-top: var(--space-6);
    }
  }

  /* Breadcrumb */
  .breadcrumb {
    max-width: var(--content-width);
    margin: 0 auto var(--space-6);
  }

  .breadcrumb__link {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .breadcrumb__link:hover {
    color: var(--color-fg);
  }

  .tool-content {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  /* Tool Header */
  .tool-header {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-header {
      margin-bottom: var(--space-12);
    }
  }

  .tool-header__badge {
    display: inline-flex;
    align-items: center;
    padding: var(--space-1) var(--space-3);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--color-success);
    background-color: oklch(from var(--color-success) l c h / 0.1);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-4);
  }

  .tool-header h1 {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-3);
  }

  @media (min-width: 768px) {
    .tool-header h1 {
      font-size: var(--text-4xl);
      margin-bottom: var(--space-4);
    }
  }

  .tool-header__intro {
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    margin: 0;
  }

  @media (min-width: 768px) {
    .tool-header__intro {
      font-size: var(--text-xl);
    }
  }

  /* Sections */
  .tool-section {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-section {
      margin-bottom: var(--space-12);
    }
  }

  .tool-section h2 {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-section h2 {
      font-size: var(--text-2xl);
    }
  }

  .tool-section h3 {
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-3);
    margin-top: var(--space-5);
  }

  @media (min-width: 768px) {
    .tool-section h3 {
      font-size: var(--text-lg);
      margin-top: var(--space-6);
    }
  }

  .tool-section p {
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  /* Requirements List */
  .requirements-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .requirements-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  .requirements-list li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  /* Setup Steps */
  .setup-step {
    margin-bottom: var(--space-6);
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  @media (min-width: 768px) {
    .setup-step {
      margin-bottom: var(--space-8);
      padding: var(--space-6);
    }
  }

  .setup-step h3 {
    margin-top: 0;
  }

  .setup-step--other {
    margin-top: var(--space-8);
    background-color: transparent;
    border-style: dashed;
  }

  .setup-step--other h3 {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
  }

  @media (min-width: 768px) {
    .setup-step--other h3 {
      font-size: var(--text-base);
    }
  }

  .setup-step ol {
    padding-left: var(--space-5);
    margin-bottom: var(--space-4);
  }

  .setup-step ol li {
    margin-bottom: var(--space-2);
    font-size: var(--text-sm);
  }

  @media (min-width: 768px) {
    .setup-step ol li {
      font-size: var(--text-base);
    }
  }

  /* Code Block */
  .code-block-wrapper {
    margin: var(--space-4) calc(-1 * var(--space-4));
    padding: 0 var(--space-4);
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }

  @media (min-width: 768px) {
    .code-block-wrapper {
      margin: var(--space-4) 0;
      padding: 0;
    }
  }

  .code-block {
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    margin: 0;
    min-width: max-content;
  }

  @media (min-width: 768px) {
    .code-block {
      min-width: auto;
    }
  }

  .code-block code {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--color-fg);
    background: none;
    padding: 0;
    white-space: pre;
  }

  @media (min-width: 768px) {
    .code-block code {
      font-size: var(--text-sm);
    }
  }

  /* Test Prompt */
  .test-prompt {
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    margin: var(--space-4) 0;
  }

  .test-prompt p {
    font-style: italic;
    color: var(--color-fg);
    margin: 0;
  }

  /* Inline Code */
  .tool-section code:not(.code-block code) {
    font-family: var(--font-mono);
    font-size: 0.85em;
    padding: var(--space-1) var(--space-2);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-default);
    color: var(--color-accent);
  }

  /* Features Section */
  .section--features {
    margin-top: var(--space-10);
  }

  @media (min-width: 768px) {
    .section--features {
      margin-top: var(--space-12);
    }
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6) 0;
  }

  .feature-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
  }

  .feature-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--color-success);
    font-weight: var(--font-bold);
  }

  .feature-note {
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-md);
    margin-bottom: 0;
  }

  .feature-note a {
    color: var(--color-accent);
  }

  /* Email Section */
  .section--email {
    padding-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .section--email {
      padding-bottom: var(--space-12);
    }
  }
</style>
