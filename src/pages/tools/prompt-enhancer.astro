---
/**
 * Prompt Enhancer Tool Page
 *
 * Setup guide for the open-source Prompt Enhancer plugin.
 * Claude Code only - no MCP server.
 * MIT licensed.
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import EmailSignup from '../../components/EmailSignup.astro';
import InstallTabs from '../../components/InstallTabs.astro';
import ToolSchema from '../../components/SEO/ToolSchema.astro';
---

<BaseLayout
  title="Prompt Enhancer — Optimize Prompts for Claude | Channel47"
  description="Optimize your prompts using Anthropic best practices. Better prompts mean better results. Open-source and MIT licensed."
>
  <ToolSchema
    name="Prompt Enhancer"
    description="Optimize prompts for Claude using Anthropic best practices."
    url="https://channel47.dev/tools/prompt-enhancer"
    version="1.0.0"
  />
  <Header />

  <main class="tool-page">
    <!-- Breadcrumb -->
    <nav class="breadcrumb">
      <a href="/tools" class="breadcrumb__link">&larr; Back to Tools</a>
    </nav>

    <article class="tool-content">
      <!-- Hero -->
      <header class="tool-header">
        <div class="tool-header__badge">Free</div>
        <h1>Prompt Enhancer</h1>
        <p class="tool-header__intro">
          Optimize your prompts using Anthropic best practices. Better prompts, better results.
        </p>
      </header>

      <!-- What You'll Need -->
      <section class="tool-section">
        <h2>What you'll need</h2>
        <ul class="requirements-list">
          <li>
            <strong>Claude Code</strong> (this is a Claude Code plugin, not an MCP server)
          </li>
          <li>
            <strong>Two minutes</strong>
          </li>
        </ul>
      </section>

      <!-- Installation -->
      <section class="tool-section">
        <h2>Installation</h2>
        <p>
          This tool is a Claude Code plugin. Install it directly from the marketplace.
        </p>

        <InstallTabs plugin="prompt-enhancer" />
      </section>

      <!-- How It Works -->
      <section class="tool-section">
        <h2>How it works</h2>
        <p>
          Once installed, use the <code>/build-prompt</code> command or ask Claude to enhance a prompt.
          The plugin applies Anthropic's documented best practices to improve clarity, specificity, and effectiveness.
        </p>
        <div class="test-prompt">
          <p>"/build-prompt"</p>
        </div>
        <p>
          The interactive wizard guides you through building an optimized prompt step by step,
          applying proven patterns from Anthropic's prompt engineering documentation.
        </p>
      </section>

      <!-- What's Included -->
      <section class="tool-section section--features">
        <h2>What's included</h2>
        <ul class="feature-list">
          <li>Interactive prompt building wizard</li>
          <li>Anthropic best practices applied automatically</li>
          <li>Structured output optimization</li>
          <li>Chain-of-thought prompting patterns</li>
          <li>Role and context framing techniques</li>
          <li>Example-based learning (few-shot) templates</li>
        </ul>
        <p class="feature-note">
          All features are open-source and MIT licensed. <a href="https://github.com/channel47/channel47/tree/main/plugins/prompt-enhancer">View on GitHub</a>
        </p>
      </section>

      <!-- Email Signup -->
      <section class="tool-section section--email">
        <EmailSignup
          title="Get updates on this tool."
          description="New features, fixes, and tips. No spam."
          buttonText="Subscribe"
          variant="card"
          tag="prompt-enhancer-tool-user"
        />
      </section>
    </article>
  </main>

  <Footer />
</BaseLayout>

<style>
  .tool-page {
    padding: var(--space-6) var(--space-4);
    padding-top: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-page {
      padding: var(--space-8) var(--space-4);
      padding-top: var(--space-6);
    }
  }

  /* Breadcrumb */
  .breadcrumb {
    max-width: var(--content-width);
    margin: 0 auto var(--space-6);
  }

  .breadcrumb__link {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .breadcrumb__link:hover {
    color: var(--color-fg);
  }

  .tool-content {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  /* Tool Header */
  .tool-header {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-header {
      margin-bottom: var(--space-12);
    }
  }

  .tool-header__badge {
    display: inline-flex;
    align-items: center;
    padding: var(--space-1) var(--space-3);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--color-success);
    background-color: oklch(from var(--color-success) l c h / 0.1);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-4);
  }

  .tool-header h1 {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-3);
  }

  @media (min-width: 768px) {
    .tool-header h1 {
      font-size: var(--text-4xl);
      margin-bottom: var(--space-4);
    }
  }

  .tool-header__intro {
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    margin: 0;
  }

  @media (min-width: 768px) {
    .tool-header__intro {
      font-size: var(--text-xl);
    }
  }

  /* Sections */
  .tool-section {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-section {
      margin-bottom: var(--space-12);
    }
  }

  .tool-section h2 {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-section h2 {
      font-size: var(--text-2xl);
    }
  }

  .tool-section p {
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  .tool-section code {
    font-family: var(--font-mono);
    font-size: 0.9em;
    padding: var(--space-1) var(--space-2);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-default);
    color: var(--color-accent);
  }

  /* Requirements List */
  .requirements-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .requirements-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  .requirements-list li::before {
    content: "\2192";
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  /* Test Prompt */
  .test-prompt {
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    margin: var(--space-4) 0;
  }

  .test-prompt p {
    font-family: var(--font-mono);
    color: var(--color-accent);
    margin: 0;
  }

  /* Features Section */
  .section--features {
    margin-top: var(--space-10);
  }

  @media (min-width: 768px) {
    .section--features {
      margin-top: var(--space-12);
    }
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6) 0;
  }

  .feature-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
  }

  .feature-list li::before {
    content: "\2713";
    position: absolute;
    left: 0;
    color: var(--color-success);
    font-weight: var(--font-bold);
  }

  .feature-note {
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-md);
    margin-bottom: 0;
  }

  .feature-note a {
    color: var(--color-accent);
  }

  /* Email Section */
  .section--email {
    padding-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .section--email {
      padding-bottom: var(--space-12);
    }
  }
</style>
