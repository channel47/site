---
/**
 * Prompt Enhancer Tool Page
 *
 * Setup guide for the open-source Prompt Enhancer plugin.
 * Claude Code only - no MCP server.
 * MIT licensed.
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import EmailSignup from '../../components/EmailSignup.astro';
import InstallTabs from '../../components/InstallTabs.astro';
import ToolSchema from '../../components/SEO/ToolSchema.astro';
---

<BaseLayout
  title="Prompt Enhancer — Optimize Prompts for Claude | Channel47"
  description="Optimize your prompts using Anthropic best practices. Better prompts mean better results. Open-source and MIT licensed."
>
  <ToolSchema
    name="Prompt Enhancer"
    description="Optimize prompts for Claude using Anthropic best practices."
    url="https://channel47.dev/tools/prompt-enhancer"
    version="1.0.0"
  />
  <Header />

  <main class="tool-page">
    <!-- Breadcrumb -->
    <nav class="breadcrumb">
      <a href="/tools" class="breadcrumb__link">&larr; Back to Tools</a>
    </nav>

    <article class="tool-content">
      <!-- Hero -->
      <header class="tool-header">
        <div class="tool-header__badge">Free</div>
        <h1>Prompt Enhancer</h1>
        <p class="tool-header__intro">
          Optimize your prompts using Anthropic best practices. Better prompts, better results.
        </p>
      </header>

      <!-- What You'll Need -->
      <section class="tool-section">
        <h2>What you'll need</h2>
        <ul class="requirements-list">
          <li>
            <strong>Claude Code</strong> (this is a Claude Code plugin, not an MCP server)
          </li>
          <li>
            <strong>Two minutes</strong>
          </li>
        </ul>
      </section>

      <!-- Installation -->
      <section class="tool-section">
        <h2>Installation</h2>
        <p>
          This tool is a Claude Code plugin. Install it directly from the marketplace.
        </p>

        <InstallTabs plugin="prompt-enhancer" />
      </section>

      <!-- How It Works -->
      <section class="tool-section">
        <h2>How it works</h2>
        <p>
          Run <code>/build-prompt</code> to start the 8-phase interactive wizard.
          Each phase builds on the previous, applying techniques matched to your specific task type.
        </p>
        <div class="test-prompt">
          <p>"/build-prompt"</p>
        </div>
        <ol class="wizard-phases">
          <li><strong>Task discovery</strong> — auto-detects task type (code, legal, creative, data-analysis, etc.)</li>
          <li><strong>Role assignment</strong> — frames Claude with the right expertise</li>
          <li><strong>Context gathering</strong> — collects background your task needs</li>
          <li><strong>Data separation</strong> — isolates data from instructions (prevents injection)</li>
          <li><strong>Output format</strong> — specifies exact structure you need</li>
          <li><strong>Technique selection</strong> — picks techniques from a task-type matrix</li>
          <li><strong>Model selection</strong> — optimizes for Sonnet, Haiku, or Opus</li>
          <li><strong>Review & export</strong> — final prompt ready to use</li>
        </ol>
      </section>

      <!-- What's Included -->
      <section class="tool-section section--features">
        <h2>What's included</h2>

        <h3>Core techniques (the 80/20)</h3>
        <ul class="feature-list">
          <li>XML tags for structure — Claude parses these natively</li>
          <li>Chain of thought — applied contextually, not everywhere</li>
          <li>Few-shot examples — templated for your task type</li>
          <li>Clear role assignment — expertise framing that works</li>
          <li>Data/instruction separation — cleaner, safer prompts</li>
        </ul>

        <h3>Claude 4.x specific optimizations</h3>
        <ul class="feature-list">
          <li>Literal interpretation handling — more explicit instructions for Claude 4</li>
          <li>Model-specific tips for Sonnet 4.5 vs Haiku 4.5</li>
          <li>Request "helpful extras" explicitly — Claude 4 won't assume</li>
        </ul>

        <h3>2025 techniques</h3>
        <ul class="feature-list">
          <li>Prefilling for response steering — start the response, Claude continues</li>
          <li>Extended thinking for complex reasoning — when you need the work shown</li>
        </ul>

        <h3>Task-type intelligence</h3>
        <ul class="feature-list">
          <li>Auto-detection of task types (code, legal, creative, data-analysis, medical, etc.)</li>
          <li>Technique selection matrix — right techniques for each task type</li>
          <li>Specialized templates per domain</li>
        </ul>

        <p class="feature-note">
          All features are open-source and MIT licensed. <a href="https://github.com/channel47/channel47/tree/main/plugins/prompt-enhancer">View on GitHub</a>
        </p>
      </section>

      <!-- Email Signup -->
      <section class="tool-section section--email">
        <EmailSignup
          title="Get updates on this tool."
          description="New features, fixes, and tips. No spam."
          buttonText="Subscribe"
          variant="card"
          tag="prompt-enhancer-tool-user"
        />
      </section>
    </article>
  </main>

  <Footer />
</BaseLayout>

<style>
  .tool-page {
    padding: var(--space-6) var(--space-4);
    padding-top: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-page {
      padding: var(--space-8) var(--space-4);
      padding-top: var(--space-6);
    }
  }

  /* Breadcrumb */
  .breadcrumb {
    max-width: var(--content-width);
    margin: 0 auto var(--space-6);
  }

  .breadcrumb__link {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out);
  }

  .breadcrumb__link:hover {
    color: var(--color-fg);
  }

  .tool-content {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  /* Tool Header */
  .tool-header {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-header {
      margin-bottom: var(--space-12);
    }
  }

  .tool-header__badge {
    display: inline-flex;
    align-items: center;
    padding: var(--space-1) var(--space-3);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--color-success);
    background-color: oklch(from var(--color-success) l c h / 0.1);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-4);
  }

  .tool-header h1 {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-3);
  }

  @media (min-width: 768px) {
    .tool-header h1 {
      font-size: var(--text-4xl);
      margin-bottom: var(--space-4);
    }
  }

  .tool-header__intro {
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    margin: 0;
  }

  @media (min-width: 768px) {
    .tool-header__intro {
      font-size: var(--text-xl);
    }
  }

  /* Sections */
  .tool-section {
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .tool-section {
      margin-bottom: var(--space-12);
    }
  }

  .tool-section h2 {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
  }

  @media (min-width: 768px) {
    .tool-section h2 {
      font-size: var(--text-2xl);
    }
  }

  .tool-section h3 {
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    margin-top: var(--space-6);
    margin-bottom: var(--space-3);
    color: var(--color-fg);
  }

  @media (min-width: 768px) {
    .tool-section h3 {
      font-size: var(--text-lg);
    }
  }

  .tool-section h3:first-of-type {
    margin-top: 0;
  }

  .tool-section p {
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  .tool-section code {
    font-family: var(--font-mono);
    font-size: 0.9em;
    padding: var(--space-1) var(--space-2);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-default);
    color: var(--color-accent);
  }

  /* Requirements List */
  .requirements-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .requirements-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-4);
    font-size: var(--text-base);
  }

  .requirements-list li::before {
    content: "\2192";
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  /* Test Prompt */
  .test-prompt {
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    margin: var(--space-4) 0;
  }

  .test-prompt p {
    font-family: var(--font-mono);
    color: var(--color-accent);
    margin: 0;
  }

  /* Wizard Phases */
  .wizard-phases {
    list-style: none;
    padding: 0;
    margin: var(--space-4) 0 0 0;
    counter-reset: phase;
  }

  .wizard-phases li {
    position: relative;
    padding-left: var(--space-8);
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    counter-increment: phase;
  }

  .wizard-phases li::before {
    content: counter(phase);
    position: absolute;
    left: 0;
    width: var(--space-5);
    height: var(--space-5);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    color: var(--color-accent);
    background-color: oklch(from var(--color-accent) l c h / 0.1);
    border-radius: var(--radius-full);
  }

  .wizard-phases li strong {
    color: var(--color-fg);
  }

  /* Features Section */
  .section--features {
    margin-top: var(--space-10);
  }

  @media (min-width: 768px) {
    .section--features {
      margin-top: var(--space-12);
    }
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-4) 0;
  }

  .feature-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-2);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
  }

  .feature-list li::before {
    content: "\2713";
    position: absolute;
    left: 0;
    color: var(--color-success);
    font-weight: var(--font-bold);
  }

  .feature-note {
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
    padding: var(--space-4);
    background-color: var(--color-bg-elevated);
    border-radius: var(--radius-md);
    margin-top: var(--space-6);
    margin-bottom: 0;
  }

  .feature-note a {
    color: var(--color-accent);
  }

  /* Email Section */
  .section--email {
    padding-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .section--email {
      padding-bottom: var(--space-12);
    }
  }
</style>
