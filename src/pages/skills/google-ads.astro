---
/**
 * Google Ads Skills Landing Page
 *
 * Convert visitors to $199 buyers.
 * Direct, confident tone. More Gary Halbert than HubSpot.
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Button from '../../components/Button.astro';
import EmailSignup from '../../components/EmailSignup.astro';
import StickyBuyBar from '../../components/StickyBuyBar.astro';
---

<BaseLayout
  title="Google Ads Skills for AI Tools — Channel47"
  description="47 GAQL queries. Audit workflows. Failure checklists. Everything I use to diagnose accounts—packaged into skill files your tools can run."
>
  <Header />

  <main class="sales-page">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero__content">
        <h1 class="hero__title" data-animate="fade-up">Steal my entire Google Ads brain for less than one billable hour.</h1>
        <p class="hero__subtitle" data-animate="fade-up">
          47 GAQL queries. Audit workflows. Failure checklists. Everything I use to diagnose accounts—packaged into skill files your tools can run.
        </p>
      </div>
    </section>

    <!-- Problem Section -->
    <section class="section section--problem">
      <div class="section__container">
        <h2 class="section__title" data-animate="fade-up">Your tools can connect to Google Ads. That's not the problem.</h2>
        <div class="prose-content" data-animate="fade-up">
          <p>
            You've got Claude. Maybe Cursor. Maybe an MCP server already wired up.
          </p>
          <p>
            You ask it to audit an account. It gives you something. But it's soft. Generic. The kind of advice you'd find in a blog post from 2019.
          </p>
          <p class="quote">
            "Consider reviewing your search terms."<br>
            "You may want to look at your quality scores."
          </p>
          <p>
            That's not an audit. That's a suggestion.
          </p>
          <p>
            The issue isn't capability. It's knowledge. The API doesn't teach judgment. The documentation doesn't tell you what actually breaks. And Google's best practices won't help you explain to a client why ROAS dropped 30% last Tuesday.
          </p>
          <p class="emphasis">You need the part that takes years to learn.</p>
        </div>
      </div>
    </section>

    <!-- What You Get Section -->
    <section class="section section--features">
      <div class="section__container">
        <h2 class="section__title" data-animate="fade-up">The 47 GAQL queries that find wasted spend before your client notices.</h2>
        <p class="section__intro" data-animate="fade-up">Organized by what actually goes wrong:</p>
        <div class="features-grid" data-animate-stagger>
          <div class="feature" data-animate="fade-up">
            <h3 class="feature__title">Search term bleed</h3>
            <p class="feature__description">
              Queries that catch irrelevant traffic you're paying for—before it becomes a pattern.
            </p>
          </div>
          <div class="feature" data-animate="fade-up">
            <h3 class="feature__title">Budget pacing failures</h3>
            <p class="feature__description">
              Queries that flag campaigns burning too fast or leaving money on the table.
            </p>
          </div>
          <div class="feature" data-animate="fade-up">
            <h3 class="feature__title">Quality score decay</h3>
            <p class="feature__description">
              Queries that surface the slow degradation most people miss until it's expensive.
            </p>
          </div>
          <div class="feature" data-animate="fade-up">
            <h3 class="feature__title">Shopping feed gaps</h3>
            <p class="feature__description">
              Queries that find missing attributes, disapprovals, and feed hygiene issues.
            </p>
          </div>
          <div class="feature" data-animate="fade-up">
            <h3 class="feature__title">Performance Max black box diagnostics</h3>
            <p class="feature__description">
              Queries that extract signal from the campaign type Google doesn't want you to see inside.
            </p>
          </div>
          <div class="feature" data-animate="fade-up">
            <h3 class="feature__title">Audience overlap and cannibalization</h3>
            <p class="feature__description">
              Queries that identify when your campaigns are bidding against themselves.
            </p>
          </div>
        </div>
        <div class="features-plus" data-animate="fade-up">
          <p>Plus:</p>
          <ul>
            <li>Audit workflows that sequence these into a repeatable process</li>
            <li>Failure checklists for the most common account problems</li>
            <li>Setup guide to get running in under 10 minutes</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- How It Works Section -->
    <section class="section section--how-it-works">
      <div class="section__container">
        <h2 class="section__title" data-animate="fade-up">Three steps. Ten minutes.</h2>
        <div class="steps" data-animate="fade-up">
          <div class="step">
            <span class="step__number">1</span>
            <div class="step__content">
              <h3 class="step__title">Connect the MCP</h3>
              <p class="step__description">
                Add the hosted URL to Claude or Cursor. No local install. No dependencies.
              </p>
            </div>
          </div>
          <div class="step">
            <span class="step__number">2</span>
            <div class="step__content">
              <h3 class="step__title">Drop in the skill files</h3>
              <p class="step__description">
                Download, unzip, place in your skills directory.
              </p>
            </div>
          </div>
          <div class="step">
            <span class="step__number">3</span>
            <div class="step__content">
              <h3 class="step__title">Run it</h3>
              <p class="step__description">
                Ask your tool to audit an account. Watch it think like a senior media buyer.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Who It's For Section -->
    <section class="section section--audience">
      <div class="section__container">
        <div class="audience-grid" data-animate-stagger>
          <div class="audience-block" data-animate="fade-up">
            <h3 class="audience-block__title">This is for you if:</h3>
            <ul class="audience-list audience-list--yes">
              <li>You manage multiple Google Ads accounts and need leverage</li>
              <li>You run an agency and want to systematize senior-level thinking</li>
              <li>You bill hourly and resent spending time on repeatable diagnostics</li>
              <li>You spend $10K+/month and can't afford to miss things</li>
            </ul>
          </div>
          <div class="audience-block" data-animate="fade-up">
            <h3 class="audience-block__title">This is not for you if:</h3>
            <ul class="audience-list audience-list--no">
              <li>You've never touched Google Ads</li>
              <li>You want done-for-you management</li>
              <li>You're not using Claude, Cursor, or similar tools yet</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Proof Section -->
    <section class="section section--proof">
      <div class="section__container">
        <h2 class="section__title" data-animate="fade-up">Who's behind this?</h2>
        <div class="prose-content" data-animate="fade-up">
          <p>
            I've spent the last ten years buying media—managing accounts at agencies, running my own e-commerce brand, consulting for companies spending $XX,XXX/month.
          </p>
          <p>
            This is the exact process I use. The queries I run. The checklists I work through.
          </p>
          <p>
            I got tired of repeating myself. So I packaged it.
          </p>
          <p class="emphasis">
            Now I use these skill files on every account I touch. They catch things faster than I do manually. And they don't forget steps.
          </p>
        </div>
      </div>
    </section>

    <!-- Pricing Section -->
    <section class="section section--pricing" id="pricing">
      <div class="section__container">
        <div class="pricing-card" data-animate="fade-up">
          <div class="pricing-card__header">
            <p class="pricing-card__label">Channel47: Google Ads Skills</p>
            <p class="pricing-card__price">$199</p>
            <p class="pricing-card__terms">One-time. No subscription. Updates included.</p>
          </div>
          <ul class="pricing-card__features">
            <li>47 GAQL query templates</li>
            <li>Audit workflows</li>
            <li>Failure checklists</li>
            <li>Setup guide</li>
            <li>Lifetime access</li>
            <li>Free updates</li>
          </ul>
          <div class="pricing-card__action">
            <Button href="#" variant="primary" size="lg" fullWidth>
              Get the Skills
            </Button>
          </div>
        </div>
      </div>
    </section>

    <!-- Email Capture Section -->
    <section class="section section--email">
      <div class="section__container">
        <div class="email-wrapper" data-animate="fade-up">
          <EmailSignup
            title="Not ready yet?"
            description="Get the free setup guide for connecting the MCP. No skills included—but you'll see how it works."
            buttonText="Send it"
            variant="card"
            tag="google-ads-interested"
          />
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="section section--faq">
      <div class="section__container">
        <h2 class="section__title" data-animate="fade-up">Questions</h2>
        <div class="faq-list" data-animate="fade-up">
          <details class="faq-item">
            <summary class="faq-item__question">Do I need to be technical?</summary>
            <div class="faq-item__answer">
              <p>
                You need to be comfortable with Claude or Cursor. If you've used either, you're fine. No coding required.
              </p>
            </div>
          </details>
          <details class="faq-item">
            <summary class="faq-item__question">What tools does this work with?</summary>
            <div class="faq-item__answer">
              <p>
                Anything that supports MCP and skill files. Claude, Cursor, and the list is growing.
              </p>
            </div>
          </details>
          <details class="faq-item">
            <summary class="faq-item__question">Is this a subscription?</summary>
            <div class="faq-item__answer">
              <p>
                No. One payment. You own it.
              </p>
            </div>
          </details>
          <details class="faq-item">
            <summary class="faq-item__question">What if I get stuck?</summary>
            <div class="faq-item__answer">
              <p>
                Email me. I'll help.
              </p>
            </div>
          </details>
          <details class="faq-item">
            <summary class="faq-item__question">Will this work for my account?</summary>
            <div class="faq-item__answer">
              <p>
                If you're running Search or Shopping campaigns, yes. Performance Max, mostly—it's a black box, but these queries extract what's available.
              </p>
            </div>
          </details>
        </div>
      </div>
    </section>
  </main>

  <Footer />

  <StickyBuyBar
    productName="Google Ads Skills"
    price="$199"
    href="#pricing"
  />
</BaseLayout>

<style>
  .sales-page {
    overflow-x: hidden;
  }

  /* Hero Section */
  .hero {
    padding: var(--space-16) var(--space-4) var(--space-10);
    text-align: center;
  }

  @media (min-width: 768px) {
    .hero {
      padding: var(--space-24) var(--space-8) var(--space-16);
    }
  }

  .hero__content {
    max-width: var(--container-md);
    margin: 0 auto;
  }

  .hero__title {
    font-size: var(--text-2xl);
    font-weight: var(--font-black);
    letter-spacing: var(--tracking-tighter);
    margin-bottom: var(--space-5);
    color: var(--color-fg);
    line-height: var(--leading-tight);
  }

  @media (min-width: 480px) {
    .hero__title {
      font-size: var(--text-3xl);
    }
  }

  @media (min-width: 768px) {
    .hero__title {
      font-size: var(--text-5xl);
      margin-bottom: var(--space-6);
      letter-spacing: var(--tracking-tightest);
    }
  }

  .hero__subtitle {
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    line-height: var(--leading-relaxed);
    max-width: 50ch;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .hero__subtitle {
      font-size: var(--text-lg);
      max-width: 60ch;
    }
  }

  /* Sections */
  .section {
    padding: var(--space-12) var(--space-4);
  }

  @media (min-width: 768px) {
    .section {
      padding: var(--space-16) var(--space-8);
    }
  }

  .section__container {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .section__title {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-6);
    text-align: center;
    line-height: var(--leading-tight);
  }

  @media (min-width: 768px) {
    .section__title {
      font-size: var(--text-3xl);
      margin-bottom: var(--space-8);
    }
  }

  .section__intro {
    text-align: center;
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    margin-bottom: var(--space-8);
  }

  /* Prose Content */
  .prose-content p {
    margin-bottom: var(--space-5);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
  }

  @media (min-width: 768px) {
    .prose-content p {
      font-size: var(--text-lg);
    }
  }

  .prose-content .quote {
    font-style: italic;
    color: var(--color-fg-muted);
    padding-left: var(--space-4);
    border-left: 3px solid var(--color-border);
  }

  .prose-content .emphasis {
    font-weight: var(--font-semibold);
    color: var(--color-fg);
  }

  /* Problem Section */
  .section--problem {
    background-color: var(--color-bg-elevated);
  }

  /* Features Section */
  .section--features {
    background-color: var(--color-bg);
  }

  .section--features .section__container {
    max-width: var(--container-lg);
  }

  .features-grid {
    display: grid;
    gap: var(--space-4);
  }

  @media (min-width: 640px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-6);
    }
  }

  @media (min-width: 1024px) {
    .features-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .feature {
    padding: var(--space-5);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
  }

  @media (min-width: 768px) {
    .feature {
      padding: var(--space-6);
    }
  }

  .feature__title {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-2);
    color: var(--color-fg);
  }

  @media (min-width: 768px) {
    .feature__title {
      font-size: var(--text-lg);
    }
  }

  .feature__description {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    margin: 0;
    line-height: var(--leading-relaxed);
  }

  @media (min-width: 768px) {
    .feature__description {
      font-size: var(--text-base);
    }
  }

  .features-plus {
    margin-top: var(--space-8);
    padding: var(--space-6);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    max-width: var(--content-width);
    margin-left: auto;
    margin-right: auto;
  }

  .features-plus p {
    font-weight: var(--font-semibold);
    color: var(--color-fg);
    margin-bottom: var(--space-3);
  }

  .features-plus ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .features-plus li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-2);
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
  }

  @media (min-width: 768px) {
    .features-plus li {
      font-size: var(--text-base);
    }
  }

  .features-plus li::before {
    content: "•";
    position: absolute;
    left: var(--space-2);
    color: var(--color-accent);
  }

  /* How It Works Section */
  .section--how-it-works {
    background-color: var(--color-bg-elevated);
  }

  .steps {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  @media (min-width: 768px) {
    .steps {
      gap: var(--space-8);
    }
  }

  .step {
    display: flex;
    gap: var(--space-4);
    align-items: flex-start;
  }

  .step__number {
    flex-shrink: 0;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-accent);
    color: var(--color-fg-inverse);
    font-family: var(--font-sans);
    font-weight: var(--font-bold);
    font-size: var(--text-base);
    border-radius: var(--radius-full);
  }

  @media (min-width: 768px) {
    .step__number {
      width: 40px;
      height: 40px;
      font-size: var(--text-lg);
    }
  }

  .step__title {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-1);
    color: var(--color-fg);
  }

  @media (min-width: 768px) {
    .step__title {
      font-size: var(--text-lg);
      margin-bottom: var(--space-2);
    }
  }

  .step__description {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    margin: 0;
    line-height: var(--leading-relaxed);
  }

  @media (min-width: 768px) {
    .step__description {
      font-size: var(--text-base);
    }
  }

  /* Audience Section */
  .section--audience {
    background-color: var(--color-bg);
  }

  .section--audience .section__container {
    max-width: var(--container-lg);
  }

  .audience-grid {
    display: grid;
    gap: var(--space-4);
  }

  @media (min-width: 768px) {
    .audience-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-8);
    }
  }

  .audience-block {
    padding: var(--space-5);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
  }

  @media (min-width: 768px) {
    .audience-block {
      padding: var(--space-6);
    }
  }

  .audience-block__title {
    font-family: var(--font-sans);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-4);
    color: var(--color-fg);
  }

  @media (min-width: 768px) {
    .audience-block__title {
      font-size: var(--text-lg);
    }
  }

  .audience-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .audience-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-3);
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    line-height: var(--leading-relaxed);
  }

  @media (min-width: 768px) {
    .audience-list li {
      font-size: var(--text-base);
    }
  }

  .audience-list li:last-child {
    margin-bottom: 0;
  }

  .audience-list li::before {
    position: absolute;
    left: 0;
    font-weight: var(--font-bold);
  }

  .audience-list--yes li::before {
    content: "✓";
    color: var(--color-success);
  }

  .audience-list--no li::before {
    content: "✕";
    color: var(--color-fg-muted);
  }

  /* Proof Section */
  .section--proof {
    background-color: var(--color-bg-elevated);
  }

  /* Pricing Section */
  .section--pricing {
    background-color: var(--color-bg);
    padding: var(--space-12) var(--space-4);
  }

  @media (min-width: 768px) {
    .section--pricing {
      padding: var(--space-20) var(--space-8);
    }
  }

  .pricing-card {
    max-width: 400px;
    margin: 0 auto;
    padding: var(--space-6);
    background-color: var(--color-bg-elevated);
    border: 2px solid var(--color-accent);
    border-radius: var(--radius-xl);
  }

  @media (min-width: 768px) {
    .pricing-card {
      padding: var(--space-8);
    }
  }

  .pricing-card__header {
    text-align: center;
    margin-bottom: var(--space-5);
    padding-bottom: var(--space-5);
    border-bottom: 1px solid var(--color-border);
  }

  .pricing-card__label {
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    color: var(--color-accent);
    margin-bottom: var(--space-2);
  }

  .pricing-card__price {
    font-family: var(--font-sans);
    font-size: var(--text-4xl);
    font-weight: var(--font-bold);
    color: var(--color-fg);
    margin-bottom: var(--space-1);
  }

  @media (min-width: 768px) {
    .pricing-card__price {
      font-size: var(--text-5xl);
    }
  }

  .pricing-card__terms {
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
    margin: 0;
  }

  .pricing-card__features {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
  }

  .pricing-card__features li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-3);
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
  }

  @media (min-width: 768px) {
    .pricing-card__features li {
      font-size: var(--text-base);
    }
  }

  .pricing-card__features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: var(--font-bold);
  }

  .pricing-card__action {
    text-align: center;
  }

  /* Email Section */
  .section--email {
    background-color: var(--color-bg-elevated);
  }

  .email-wrapper {
    max-width: var(--container-sm);
    margin: 0 auto;
  }

  /* FAQ Section */
  .section--faq {
    background-color: var(--color-bg);
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  @media (min-width: 768px) {
    .faq-list {
      gap: var(--space-4);
    }
  }

  .faq-item {
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    overflow: hidden;
    transition: border-color var(--duration-fast) var(--ease-out);
  }

  .faq-item[open] {
    border-color: var(--color-accent);
  }

  .faq-item__question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    padding: var(--space-4);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    color: var(--color-fg);
    cursor: pointer;
    list-style: none;
    min-height: 60px;
    -webkit-tap-highlight-color: transparent;
    transition: background-color var(--duration-fast) var(--ease-out);
  }

  .faq-item__question:hover {
    background-color: var(--color-bg-hover);
  }

  @media (min-width: 768px) {
    .faq-item__question {
      padding: var(--space-5) var(--space-6);
      font-size: var(--text-base);
    }
  }

  .faq-item__question::-webkit-details-marker {
    display: none;
  }

  .faq-item__question::after {
    content: "+";
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--text-xl);
    font-weight: var(--font-normal);
    color: var(--color-fg-muted);
    background-color: var(--color-bg);
    border-radius: var(--radius-full);
    transition:
      transform var(--duration-fast) var(--ease-out),
      background-color var(--duration-fast) var(--ease-out);
  }

  .faq-item[open] .faq-item__question::after {
    content: "−";
    background-color: var(--color-accent-subtle);
    color: var(--color-accent);
  }

  .faq-item__answer {
    padding: 0 var(--space-4) var(--space-5);
  }

  @media (min-width: 768px) {
    .faq-item__answer {
      padding: 0 var(--space-6) var(--space-6);
    }
  }

  .faq-item__answer p {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    margin: 0;
    line-height: var(--leading-relaxed);
  }

  @media (min-width: 768px) {
    .faq-item__answer p {
      font-size: var(--text-base);
    }
  }
</style>
