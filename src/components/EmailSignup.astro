---
/**
 * EmailSignup Component
 *
 * Minimal inline email capture form. No card styling, no containers.
 * Just input + button that blends into page content.
 */

interface Props {
  buttonText?: string;
  placeholder?: string;
  tag?: string;
  helperText?: string;
  class?: string;
}

const {
  buttonText = 'Subscribe',
  placeholder = 'you@example.com',
  tag = 'general',
  helperText = 'No spam. Unsubscribe anytime.',
  class: className = ''
} = Astro.props;

// Generate unique ID for this form instance
const formId = `email-signup-${Math.random().toString(36).substr(2, 9)}`;
---

<form
  id={formId}
  class:list={['email-signup', className]}
  data-signup-form
  data-tag={tag}
>
  <div class="email-signup__row">
    <label for={`${formId}-email`} class="visually-hidden">Email address</label>
    <input
      type="email"
      id={`${formId}-email`}
      name="email"
      placeholder={placeholder}
      required
      class="email-signup__input"
    />
    <button type="submit" class="email-signup__button">
      <span class="email-signup__button-text">{buttonText}</span>
      <span class="email-signup__button-loading" aria-hidden="true">...</span>
    </button>
  </div>
  <div class="email-signup__feedback">
    <div class="email-signup__message" role="status" aria-live="polite"></div>
    {helperText && <p class="email-signup__helper">{helperText}</p>}
  </div>
</form>

<script>
  document.addEventListener('astro:page-load', () => {
    const forms = document.querySelectorAll('[data-signup-form]');

    const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    function validateEmail(email: string): boolean {
      if (!email || email.length > 254) return false;
      return EMAIL_REGEX.test(email.trim());
    }

    forms.forEach((form) => {
      if (form.hasAttribute('data-initialized')) return;
      form.setAttribute('data-initialized', 'true');

      const formElement = form as HTMLFormElement;
      const emailInput = formElement.querySelector('input[name="email"]') as HTMLInputElement;

      emailInput.addEventListener('blur', () => {
        const email = emailInput.value.trim();
        if (email && !validateEmail(email)) {
          emailInput.setCustomValidity('Please enter a valid email address');
          emailInput.reportValidity();
        } else {
          emailInput.setCustomValidity('');
        }
      });

      emailInput.addEventListener('input', () => {
        emailInput.setCustomValidity('');
      });

      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        const submitButton = formElement.querySelector('button[type="submit"]') as HTMLButtonElement;
        const buttonText = submitButton.querySelector('.email-signup__button-text') as HTMLElement;
        const buttonLoading = submitButton.querySelector('.email-signup__button-loading') as HTMLElement;
        const messageDiv = formElement.querySelector('.email-signup__message') as HTMLElement;
        const helperText = formElement.querySelector('.email-signup__helper') as HTMLElement;
        const tag = formElement.dataset.tag;

        messageDiv.textContent = '';
        messageDiv.className = 'email-signup__message';

        const email = emailInput.value.trim();
        if (!validateEmail(email)) {
          messageDiv.textContent = 'Please enter a valid email address';
          messageDiv.classList.add('email-signup__message--error');
          emailInput.focus();
          return;
        }

        submitButton.disabled = true;
        buttonText.style.display = 'none';
        buttonLoading.style.display = 'inline';
        if (helperText) helperText.style.display = 'none';

        try {
          const response = await fetch('/api/subscribe', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, tag: tag || 'general' }),
          });

          const data = await response.json();

          if (response.ok) {
            messageDiv.textContent = data.message || 'Successfully subscribed!';
            messageDiv.classList.add('email-signup__message--success');
            emailInput.value = '';
          } else {
            messageDiv.textContent = data.message || 'Subscription failed. Please try again.';
            messageDiv.classList.add('email-signup__message--error');
            if (helperText) helperText.style.display = '';
          }
        } catch (error) {
          messageDiv.textContent = 'An error occurred. Please try again later.';
          messageDiv.classList.add('email-signup__message--error');
          if (helperText) helperText.style.display = '';
        } finally {
          submitButton.disabled = false;
          buttonText.style.display = '';
          buttonLoading.style.display = 'none';
        }
      });
    });
  });
</script>

<style>
  .email-signup {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    width: 100%;
    max-width: 400px;
  }

  /* Input + Button row */
  .email-signup__row {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  @media (min-width: 480px) {
    .email-signup__row {
      flex-direction: row;
    }
  }

  /* Input - compact and clean */
  .email-signup__input {
    flex: 1;
    height: var(--input-height);
    padding: 0 var(--input-padding-x);
    font-family: var(--font-sans);
    font-size: var(--input-font-size);
    color: var(--color-fg);
    background: var(--input-bg);
    border: var(--input-border);
    border-radius: var(--input-radius);
    outline: none;
    min-width: 0;
    -webkit-tap-highlight-color: transparent;
    transition:
      border-color var(--duration-fast) var(--ease-out),
      box-shadow var(--duration-fast) var(--ease-out);
  }

  .email-signup__input::placeholder {
    color: var(--color-fg-muted);
  }

  .email-signup__input:focus {
    border-color: var(--input-focus-border);
    box-shadow: var(--input-focus-ring);
  }

  /* Button - compact and accent */
  .email-signup__button {
    height: var(--button-height);
    padding: 0 var(--button-padding-x);
    font-family: var(--font-sans);
    font-size: var(--input-font-size);
    font-weight: var(--font-semibold);
    color: var(--color-fg-inverse);
    background-color: var(--color-accent);
    border: none;
    border-radius: var(--input-radius);
    cursor: pointer;
    white-space: nowrap;
    -webkit-tap-highlight-color: transparent;
    transition:
      background-color var(--duration-fast) var(--ease-out),
      transform var(--duration-fast) var(--ease-out);
  }

  .email-signup__button:hover {
    background-color: var(--color-accent-hover);
  }

  .email-signup__button:active {
    transform: scale(0.97);
  }

  .email-signup__button:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  .email-signup__button:disabled {
    opacity: 0.7;
    cursor: not-allowed;
  }

  .email-signup__button-loading {
    display: none;
    animation: pulse 1.5s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  /* Feedback area */
  .email-signup__feedback {
    min-height: 1.25rem;
  }

  .email-signup__message {
    font-size: var(--text-xs);
    margin: 0;
  }

  .email-signup__message:empty {
    display: none;
  }

  .email-signup__message--success {
    color: var(--color-success);
  }

  .email-signup__message--error {
    color: var(--color-error);
  }

  .email-signup__helper {
    font-size: var(--text-xs);
    color: var(--color-fg-muted);
    margin: 0;
  }

  .email-signup__message:not(:empty) + .email-signup__helper {
    display: none;
  }

  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .email-signup__input,
    .email-signup__button {
      transition: none;
    }

    .email-signup__button:active {
      transform: none;
    }
  }
</style>
