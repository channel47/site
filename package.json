{
  "name": "channel47",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync-plugins": "npx tsx scripts/sync-plugins.ts",
    "predev": "npm run sync-plugins",
    "dev": "astro dev",
    "prebuild": "npm run sync-plugins",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.1.6",
    "astro": "^4.16.17"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
